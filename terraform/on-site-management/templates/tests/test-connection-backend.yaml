apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "on-site-management.fullname" . }}-test-connection-backend"
  labels:
{{ include "on-site-management.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "on-site-management.fullname" . }}-backend:{{ .Values.backend.service.port }}']
  restartPolicy: Never
